{
  "name": "nexusguard",
  "version": "0.1.0",
  "private": true,
  "description": "AI Governance & API Proxying SaaS — Multi-tenant Platform",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=apps/web",
    "build": "npm run build --workspace=apps/web",
    "lint": "npm run lint --workspace=apps/web",
    "start": "npm run start --workspace=apps/web"
  },
  "engines": {
    "node": ">=22.0.0"
  }
}
